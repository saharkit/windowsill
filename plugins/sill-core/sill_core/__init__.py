"""sill-core — shared core modules for windowsill plugins.

This package ships plugin-agnostic modules that other plugins import.
The root belongs to the shelf; each plugin owns its own subtree.
"""

from __future__ import annotations
