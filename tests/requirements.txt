# Test dependencies for the server. Deliberately NOT the full runtime: no faster-whisper, no
# ruaccent, no ukrainian-word-stress, no models — the tests fake those seams. Only the pieces whose
# real behaviour is part of the contract are installed (torch tensors, soundfile's WAV encoding,
# FastAPI's request handling).
#
# Install torch from the CPU index first on Linux:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r tests/requirements.txt

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0   # imported by the module under test (no [standard] extras needed)
python-multipart>=0.0.9
torch>=2.2
soundfile>=0.12
numpy>=1.26
httpx>=0.27          # FastAPI's TestClient runs on it
pytest>=8.0
pytest-cov>=5.0
